spring:
  application:
    name: spring-web-demo
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:./appdb
    username: sa
    password:
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

server:
  port: 8085

